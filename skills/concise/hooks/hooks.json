{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PLUGIN_ROOT/hooks/inject-core.sh\" concise-core-override.md core.md",
            "statusMessage": "Loading the concise style",
            "timeout": 10
          },
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PLUGIN_ROOT/hooks/self-update.sh\" concise",
            "async": true,
            "timeout": 30
          },
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PLUGIN_ROOT/hooks/notices.sh\" concise \"concise is on. Commands: /concise:rewrite retrofits a finished text, /concise:pr writes the PR description from the diff, /concise:card writes a task that stands alone, /concise:commit writes the message for what is staged. The audit agent reports violations without rewriting. This note shows once.\" \"concise: self-update has been failing for over a week. Run %s to see the error.\"",
            "timeout": 10
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PLUGIN_ROOT/hooks/credit-guard.sh\" \"AI credit found in the text. The concise rule bans crediting an AI agent in commits, PRs, issues and releases - rewrite without it.\" .concise-no-credit-guard",
            "timeout": 10
          }
        ]
      },
      {
        "matcher": "PowerShell",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PLUGIN_ROOT/hooks/credit-guard.sh\" \"AI credit found in the text. The concise rule bans crediting an AI agent in commits, PRs, issues and releases - rewrite without it.\" .concise-no-credit-guard",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
