<!--
First line: what this PR does — no header above it.
Then only what the reviewer can't get faster from the diff: what changes for
whoever uses this, decisions with their costs, what was left out of scope,
anything unverified (named as unverified). Several deliverables read as a
short list, one line each, label first. Many files? Say where to start
reading.

If a plugin changed, the version bumped and CHANGELOG.md gained a line
(scripts/check-bump.sh enforces the first half).
-->

<!-- How to test — keep this section, filled:
1. The exact copy-pasteable command.
2. What appears if it's right.
3. What would prove it broke, when step 1 doesn't cover it.
Say inside the step when you did not run it. -->

Como testar:

1.
